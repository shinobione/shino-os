# Legacy entry point intentionally retired: it killed arbitrary port owners and
# defaulted to 8000. Cleanup is now ownership-checked in shino.ps1 run/api.
throw 'Use shino.ps1 run/api; standalone port reclamation is disabled.'
